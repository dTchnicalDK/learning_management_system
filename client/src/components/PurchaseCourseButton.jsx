import React from "react";
import { Button } from "./ui/button";

const PurchaseCourseButton = () => {
  return (
    <div>
      <Button variant="default" className="text-xl my-2 cursor-pointer">
        Buy Course Now
      </Button>
    </div>
  );
};

export default PurchaseCourseButton;
